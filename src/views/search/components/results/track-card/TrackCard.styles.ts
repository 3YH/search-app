import { Card } from '@mui/material';
import { styled } from '@mui/system';

const TrackCard = styled(Card)({
  display: 'flex',
  alignSelf: 'center',
  width: 350,
  height: 60,
});

export { TrackCard };
